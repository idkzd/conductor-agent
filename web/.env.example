# For live on-chain features (LIVE LEDGER etc.)
NEXT_PUBLIC_DECISION_LOGGER_ADDRESS=0x40E51Bdc032F31cb394BBCCF63f66Ac65CAd8807

# Optional for real writes from UI (needs private key with funds)
# PRIVATE_KEY=0x...

# === Real LLM (optional, for richer agent reasoning on the public demo) ===
# Use a free model on OpenRouter (sign up at openrouter.ai, get API key).
# Recommended free model: openrouter/owl-alpha (or any :free tier you prefer)
# If not set, the API uses high-quality deterministic simulation (always works, zero cost).
OPENROUTER_API_KEY=sk-or-...
